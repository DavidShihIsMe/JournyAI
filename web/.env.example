OPENAI_API_KEY=fake-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
# Optional: verify walking minutes (hotel → stops, venue legs) with Google Directions API after the AI draft.
# Create a key with "Directions API" enabled. Server-side only — do not expose as NEXT_PUBLIC_*.
GOOGLE_MAPS_API_KEY=
# Optional: auto-lookup flight departure/arrival times from the plan form.
# Get a free key at aviationstack.com. Server-side only — do not expose as NEXT_PUBLIC_*.
AVIATIONSTACK_API_KEY=
